name: Mainnet-Staging
on:
  workflow_dispatch:
    branches:
      - main
      - kepler

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      AWS_ACCESS_KEY_ID: ${{ vars.AWS_ACCESS_KEY_ID_V2 }}
      AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY_V2 }}
      VITE_SENTRY_DSN: ${{ vars.DEV_SENTRY_DSN }}
      NODE_OPTIONS: --max-old-space-size=32768
    steps:
      - uses: actions/checkout@v5

      - name: Setup Node.js environment
        uses: actions/setup-node@v5
        with:
          node-version: 22

      - run: yarn

      - name: Build
        # Disable CI flag so linter warnings aren't treated as errors
        run: CI=false yarn build:staging

      - name: Deploy Staging
        uses: reggionick/s3-deploy@v3
        with:
          folder: dist
          bucket: ${{ vars.S3_BUCKET_STAGING }}
          bucket-region: ap-northeast-1
          invalidation: /*
          dist-id: ${{ secrets.CLOUDFRONT_ID_KEPLER_STAGING }}
          delete-removed: false
          no-cache: false
          private: true
